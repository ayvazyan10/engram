{
  "name": "@engram-ai-memory/vis",
  "version": "0.5.0",
  "description": "Visualization helpers — neural graph layout, animation engine, color mapping",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@engram-ai-memory/eslint-config": "workspace:*",
    "@engram-ai-memory/tsconfig": "workspace:*",
    "@vitest/coverage-v8": "^3.1.1",
    "eslint": "^9.39.0",
    "typescript": "5.7.3",
    "vitest": "^3.1.1"
  }
}
