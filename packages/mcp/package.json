{
  "name": "@neural-core/mcp",
  "version": "0.1.0",
  "description": "MCP Server — connects NeuralCore brain to Claude Code and any MCP client",
  "main": "./dist/server.js",
  "bin": {
    "neural-core-mcp": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "@neural-core/core": "workspace:*",
    "uuid": "^11.1.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@neural-core/tsconfig": "workspace:*",
    "@types/uuid": "^10.0.0",
    "typescript": "5.7.3"
  }
}
